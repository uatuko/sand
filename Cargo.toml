[package]
name = "sand"
version = "0.1.0"
edition = "2021"

[dependencies]
ctrlc = { version = "3", features = ["termination"] }
tokio = { version = "1", features = ["macros", "net", "rt-multi-thread"] }
